@model AddPhoneNumberViewModel
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["Add Phone Number"];
}

<a asp-controller="Manage" asp-action="Index" class="btn">&lt;- @Localizer["Profil"]</a>
<div class="row login-form">
    <div class="col-md-12">
        <div class="col-md-6 col-md-offset-3">
            <h2>@ViewData["Title"].</h2>
            <form asp-controller="Manage" asp-action="AddPhoneNumber" method="post" class="form-horizontal">
                <h4>@Localizer["Add a phone number"].</h4>
                <hr />
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group">
                    <input asp-for="PhoneNumber" placeholder="@Localizer["PhoneNumber"]" class="form-control" />
                    <span asp-validation-for="PhoneNumber" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <div class="row">
                        <div class="col-md-12">
                            <button type="submit" class="form-control btn btn-login">@Localizer["Submit"]</button>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
